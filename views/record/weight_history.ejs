<!DOCTYPE html>
<html>
<head>
    <title>个人身高记录</title>
</head>
<body>
    <div >
        <div>个人身高记录</div>
        <p>历史记录(<%= list.length %>)</p>
        <ul>
            <% list.forEach(function(item){%>
                <li><span><%=item.date %> </span> 体重：<span><%=item.value %></span>cm</li>
            <% }) %>
        </ul>
    </div>
</body>
</html>